services:
  lunchinator:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      DATABASE_PATH: /app/data/lunchinator.db
    restart: unless-stopped
